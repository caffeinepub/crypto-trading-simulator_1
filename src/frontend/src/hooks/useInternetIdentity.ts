/**
 * Re-export of useInternetIdentity from the core-infrastructure package.
 * This provides Internet Identity login/logout and identity access.
 */
export {
  useInternetIdentity,
  InternetIdentityProvider,
  type InternetIdentityContext,
  type Status,
} from "@caffeineai/core-infrastructure";
